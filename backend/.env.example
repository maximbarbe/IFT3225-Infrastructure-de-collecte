ATLAS_URI=<mongodb url>
JWT_SECRET=<jwt secret>

# Optionnel. Si absent, le cache serveur est un cache en memoire par processus.
# Sur Render: ajouter cette variable dans Environment, avec l'Internal Redis URL
# du service Key Value / Redis (format redis://... ou rediss://...).
REDIS_URL=<url redis>
